---
applications:
- name: YOUR_APP_NAME
  host: YOUR_HOST_NAME
  domain: mybluemix.net
  memory: 512M
  instances: 1
  path: .
  command: node index --production
  env:
    NODE_ENV: production
  services:
  - ghost-cleardb
  - ghost-cloudant
